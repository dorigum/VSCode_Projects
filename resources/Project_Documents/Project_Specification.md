# ☕ 카페 키오스크 프로젝트 명세서 (Cafe Kiosk Project)

## 1. 프로젝트 개요
- **목표**: MVC 패턴을 적용한 콘솔 기반 카페 키오스크 시스템 구축 (Layered Architecture)
- **주요 기능**: 회원/비회원 주문 서비스 및 관리자 모드 (상품/카테고리/회원/매출 관리)
- **개발 환경**: Java 18 (OpenJDK), **MySQL 8.0**, JDBC (mysql-connector-j-9.6.0)
---
## 2. 주요 기능 요구사항 (진행 현황)

### 👤 사용자(회원/비회원) 기능
- [x] **회원 로그인**: 휴대폰 번호/비밀번호 인증 (`MemberRepository` 연동)
- [x] **회원가입 보너스**: 신규 회원가입 시 **1,000 포인트 자동 적립** 로직 구현 (신규)
- [x] **주문 내역 조회**: JOIN 쿼리를 통한 상세 내역 확인
- [x] **포인트 내역 확인**: 본인의 포인트 적립 및 차감 히스토리(사유 포함) 조회 기능 (신규)
- [ ] 상품 선택 및 장바구니 담기 (구현 예정)
- [ ] 결제 처리 및 포인트 적립 (구현 예정)

### 🛠️ 관리자(Admin) 통합 관리 시스템
- [x] **카테고리 관리 (CRUD)**: 카테고리 추가, 목록 조회, 삭제
- [x] **메뉴 및 옵션 관리 (CRUD)**: 
    - **메뉴 정보 관리**: 카테고리별 상품 등록, 조회, 삭제
    - **메뉴 옵션 관리**: 옵션 그룹(온도, 사이즈 등) 및 세부 옵션(ICE, Regular 등) 관리 기능 추가
    - **카테고리별 옵션 설정**: 각 카테고리에 기본 적용될 옵션 그룹 매핑 설정 기능 추가
- [x] **회원 관리 및 보안**: 
    - 가입된 전체 회원 목록 조회 및 특정 회원 삭제
    - **포인트 수정 고도화**: 특정 회원에게 포인트 지급/차감 시 **수정 사유**를 필수 입력하도록 개선 (신규)
    - **등급(Role) 관리**: 일반 회원(USER)과 관리자(ADMIN) 등급 변경 기능 추가 (신규)
    - **1인 관리자 체제**: 시스템 안정성을 위해 관리자는 **단 1명만 존재**할 수 있도록 제한 로직 구현 (신규)
    - **관리자 보호 로직**: 관리자 계정은 포인트 수정 및 직접 삭제가 불가능하도록 안전 장치 마련 (신규)
- [x] **매출 통계 및 분석**:
    - 누적 총 매출액 집계
    - **카테고리별 매출 분석** (Coffee, Tea 등)
    - **인기 메뉴 Top 3** 선정 (판매량 기준)
    - **일별 매출 추이 시각화** (최근 7일 막대 그래프)
    - **기간별 상세 조회**: 날짜 범위 지정을 통한 매출액 및 **객단가(AVG)** 분석 (신규)
    - **시간대별 매출 분석**: 하루 중 주문 집중 시간대(피크타임) 파악 (신규)
    - **우수 회원 기여도 분석**: 누적 결제액 기준 상위 **VVIP 회원** 추출 (신규)
- [x] **주문 관리**: 전체 주문 목록 확인 및 **결제 취소(CANCELLED)** 처리
---
## 3. 시스템 아키텍처 (Layered Architecture)
- **Model**: `Menu`, `Member`, `Order`, `MenuOption`, `OptionGroup`, `PointHistory` (신규)
- **View**: `StartView` (진입점), `MenuView` (메인 루프), `EndView` (출력 전담), `FailView` (에러 전담), `OrderingView` (주문 전담)
- **Controller**: `AdminController`, `MenuController`, `MemberController`
- **Service**: `AdminService`, `MemberService`, `MenuService`
- **Repository**: `MenuRepository`, `MemberRepository`, `CategoryRepository`, `OrderRepository`, `MenuOptionRepository`, `OptionGroupRepository`
- **Configuration**: `dbInfo.properties` (파일명 대소문자 수정 및 소스 폴더 경로 최적화)
---
## 4. 데이터 설계 (MySQL 테이블 구조)
| 구분 | 테이블명 | 설명 |
| :--- | :--- | :--- |
| **Category** | CATEGORY | 상품 분류 (Coffee, Tea, Dessert 등) |
| **Menu** | MENU | 상품명, 가격, 설명, 품절 여부 |
| **MenuOption** | MENU_OPTION | 세부 메뉴 옵션 (HOT, ICE, Regular, Large 등) |
| **OptionGroup** | OPTION_GROUP | 옵션 그룹 (온도, 사이즈, 카페인유무 등) |
| **CategoryOption** | CATEGORY_OPTION_GROUP | 카테고리별 기본 적용 옵션 그룹 매핑 |
| **Mapping** | MENU_OPTION_GROUP | 메뉴별 적용 가능한 옵션 그룹 매핑 |
| **Member** | MEMBER | 회원 정보, 휴대폰 번호(Unique), 포인트, 역할(ADMIN/USER) |
| **PointHistory** | POINT_HISTORY | 포인트 변동 내역 및 사유 기록 테이블 (신규) |
| **Order** | ORDERS | 주문 총액, 사용/적립 포인트, 주문 상태, 주문 일시 |
| **OrderItem** | ORDER_ITEM | 주문 상세(수량, 단가), 메뉴/카테고리명 스냅샷 포함 |
| **OrderItemOption**| ORDER_ITEM_OPTION | 주문 시점에 선택된 세부 옵션 기록 |
| **Wishlist** | WISHLIST | 회원의 찜 목록 |

---
## 5. 진행 현황 및 히스토리(최신 업데이트 순)

### [2026-03-18] 관리자 보안성 강화 및 UI 미세 조정
- **관리자 포인트 정보 노출 제한**: 
    - 보안 및 비즈니스 로직 일관성을 위해 관리자(`ADMIN`) 계정 로그인 시 보유 포인트 내역이 화면에 출력되지 않도록 `EndView` 로직 수정.
    - 관리자는 포인트 수정 주체이므로 본인의 포인트 정보 노출이 불필요하다는 정책 반영.
- **UI 텍스트 가독성 개선**:
    - 메뉴 품절 여부 표시(`availability`)의 공백 및 문자열 정렬을 최적화하여 콘솔 가독성 향상.

### [2026-03-17] 대규모 샘플 데이터 기반 매출 통계 시각화 고도화 및 비즈니스 분석(BI) 기능 강화
- **최신 비즈니스 인텔리전스(BI) 기능 도입**:
    - **시간대별(Hourly) 매출 피크타임 분석**: 0시부터 23시까지의 매출 분포를 막대 그래프로 시각화하여 운영 효율화 지표 제공.
    - **요일별(Day of Week) 매출 분석**: 월~일 요일별 매출 비중을 계산하여 주간 운영 전략 수립 지원.
    - **통합 피크타임 분석 리포트**: 시간대별 및 요일별 데이터를 한 화면에 결합한 '통합 분석 모드' 구현.
    - **VVIP 우수 고객 분석**: 누적 결제 금액 기준 상위 N명의 회원 리스트와 총 구매액을 추출하여 고객 타겟팅 기반 마련.
- **매출 통계 시각화 (Relative Scaling) 고도화**:
    - **상대적 스케일링 로직**: 현재 조회 기간 내 '최대 매출액'을 기준으로 그래프 길이를 결정하여 데이터 간 변별력을 극대화함.
    - **동적 UI 패딩**: 데이터 수치가 커지거나 기간이 길어져도 UI 레이아웃이 깨지지 않도록 가변 폭 정밀 조정.
- **포인트 시스템 고도화 및 버그 수정**:
    - **수정 사유 필수 기록**: 관리자가 수동으로 포인트 조정 시 사유(Reason)를 반드시 입력하게 하여 데이터 추적성 확보.
    - **신규 가입 포인트 즉시 적립**: 회원가입 시 1,000 포인트가 누락 없이 즉시 적립되도록 로직 무결성 검증 완료.
- **데이터 활용성 및 호환성 최적화**:
    - **통계 데이터 CSV 내보내기**: 모든 분석 데이터를 엑셀 호환 형식으로 추출하며, `MS949` 인코딩을 적용해 한글 깨짐 방지.
    - **최신 트렌드 우선 조회**: 모든 통계 조회 시 '가장 최신 기간'을 우선적으로 가져오도록 쿼리 로직 최적화.

### [2026-03-16] 회원 정보 가입일자 추가, 주문 상세 옵션 조회 및 관리자 UI 고도화 (UX/UI Enhancement)
- **관리자 전용 UI 최적화**:
    - **메뉴 조회 가이드 제거**: 관리자 모드의 메뉴 목록 출력 시 불필요한 주문 가이드(0. 뒤로, 8. 카트확인, 9. 주문하기)를 제거하여 관리 업무에만 집중할 수 있도록 UI 간소화.
    - **옵션 그룹 조회 로직 강화**: 동일한 이름의 옵션 그룹이 존재할 경우 하나로 통합하여 출력하도록 `OptionGroupRepositoryImpl.findAll` 쿼리 고도화 (중복 출력 버그 해결).
- **회원 관리 시스템 시각화 개선**:
    - **가입 일자 노출**: 회원 목록 조회 시 `MEMBER` 테이블의 `created_at` 데이터를 활용하여 가입 날짜를 'yyyy-MM-dd' 형식으로 출력하도록 개선.
    - **표 형식 UI 도입**: 기존 단순 나열 방식에서 `ID | 전화번호 | 보유 포인트 | 등급 | 가입일자` 형태의 관리자 전용 표 형식으로 개편하여 가독성 극대화.
- **주문 상세 내역 연동 강화 (E2E Option Tracking)**:
    - **데이터 로딩 최적화**: `MemberRepositoryImpl` 및 `OrderRepositoryImpl`의 조회 로직을 수정하여, 주문 내역 조회 시 `ORDER_ITEM_OPTION` 테이블을 JOIN해 사용자가 선택한 **실제 세부 옵션 정보**를 완벽하게 로드하도록 구현.
    - **UI 출력 강화**: 회원 및 관리자 주문 관리 화면에서 각 메뉴 아래에 `▶ [선택된 상세 옵션: ...]` 형태로 옵션 정보를 명확히 표시하여 주문 무결성 확인 가능.
- **포인트 히스토리 및 데이터 무결성 보완**:
    - **포인트 내역 시각화**: 포인트 변동 내역을 `일시 | 변동금액 | 사유` 형태의 표 형식으로 출력하고, 내역이 없을 경우 친절한 안내 메시지 노출.
    - **중복 방지 로직**: 옵션 그룹 등록 시 동일한 이름의 그룹이 중복 생성되지 않도록 `AdminServiceImpl`에 검증 로직 추가.
    - **상태 한글화**: 관리자 화면의 주문 상태(`COMPLETED`, `CANCELLED`)를 `[정상결제]`, `[취소완료]`로 한글화하여 직관성 개선.

### [2026-03-16] 카테고리 기반 메뉴 옵션 템플릿 방식 도입 및 메뉴별 전용 옵션 예외 처리 기능 고도화 (Critical Update)
- **카테고리 기반 메뉴 옵션 템플릿(Template) 방식 구현**:
    - **메뉴 등록 자동화**: 메뉴 등록 시 해당 카테고리에 설정된 기본 옵션 그룹들이 `MENU_OPTION_GROUP`에 자동으로 복제되어 등록되도록 `AdminServiceImpl.registerMenu` 로직 개선.
    - **데이터 관리 효율화**: 관리자가 매번 수동으로 옵션을 연결할 필요 없이, 카테고리 설정만으로 수많은 메뉴에 공통 옵션을 일괄 적용할 수 있는 기반 마련.
- **메뉴별 전용 옵션 예외 처리(Override) 기능 강화**:
    - **우선순위 기반 옵션 로딩**: `MenuRepositoryImpl.fetchOptionGroups`에서 메뉴별 전용 옵션(`MENU_OPTION_GROUP`)이 있으면 우선 사용하고, 없을 경우에만 카테고리 기본값(`CATEGORY_OPTION_GROUP`)을 사용하는 하이브리드 로직 적용.
    - **개별 옵션 삭제 기능 구현**: 특정 메뉴(예: 청포도 에이드)에서 불필요한 공통 옵션(예: 온도)을 삭제할 수 있도록 `removeOptionGroupFromMenu` 메서드 및 UI 연동 완료.
- **관리자 UI/UX 편의성 증대**:
    - **실시간 옵션 모니터링**: 관리자 메뉴 목록 출력 시 각 메뉴에 현재 연결된 모든 옵션 그룹을 실시간으로 함께 노출하여 설정 상태 확인 용이성 확보.
    - **대화형 메뉴-옵션 설정**: 메뉴 등록 직후 세부 옵션 설정을 바로 진행할 수 있는 인터랙티브 가이드 프로세스 도입.

### [2026-03-16] 팀 프로젝트 통합, 저장소 이전 및 시스템 안정화 (Critical Update)
- **원격 저장소 이전 및 환경 재설정**:
    - **레포지토리 주소 변경**: `VSCode_Projects`에서 `1_Cafe_kiosk_Projects`로 원격 저장소 URL 변경 반영 및 `origin` 설정 업데이트 완료.
    - **README.md 복구**: 깨진 인코딩 및 누락된 DB 접속 가이드 내용을 최신 버전으로 복구 및 커밋 메시지 정규화.
- **팀 브랜치 통합 및 충돌 해결 (Feature Merge)**:
    - **`member-point` 브랜치 병합**: 다른 팀원이 작업한 '회원 포인트 및 추천 메뉴' 기능을 현재 브랜치(`feature/cafe-admin`)에 통합 완료.
    - **대규모 충돌 해결**: `MemberRepositoryImpl`, `MemberController`, `OrderingView` 등 8개 공통 파일에서 발생한 로직 충돌을 정밀 분석하여 두 팀원의 기능을 모두 보존하며 병합 성공.
- **회원 및 주문 시스템 고도화 (Final Fix)**:
    - **주문자 식별 오류 완벽 해결**: 로그인한 회원이 주문 시 '비회원'으로 기록되던 고질적인 버그를 **명시적 컬럼 매핑(Explicit Column Mapping)**과 **회원번호 추출 로직 강화**를 통해 최종 해결.
    - **주문 내역 가독성 개선**: 본인의 주문 내역 조회 시 전화번호 정보가 누락되지 않도록 `Order` 객체 생성 구조를 개선하여 주문자 정보를 실시간 노출.
    - **포인트 히스토리 자동 기록**: 주문 과정에서 발생하는 포인트 사용 및 적립 내역이 `POINT_HISTORY` 테이블에 실시간으로 기록되도록 트랜잭션 로직 확장.
    - **가입 포인트 로직 보완**: 선호 카테고리 선택형 회원가입 시에도 축하 포인트(1,000P)가 누락 없이 적립되도록 `MemberService` 로직 수정.
- **버그 수정 및 DB 안정화**:
    - **UI 중복 출력 제거**: `MenuView`에서 로그인 후 메뉴 옵션이 중복으로 나타나던 현상 해결.
    - **팀 사양 준수**: 팀의 결정에 따라 회원 메뉴에서 '찜 목록'을 제거하고 퀵오더/추천메뉴 중심으로 번호 재정렬 완료.
    - **모델 중복 정의 수정**: `OrderItem.java` 내 중복된 세터 메서드 제거 및 컴파일 오류 해결.
    - **DB 자동 구축 강화**: `POINT_HISTORY` 테이블이 없을 경우 조회/저장 시점에 자동으로 생성하는 `CREATE TABLE IF NOT EXISTS` 로직을 Repository 전반에 적용하여 런타임 에러 방지.
- **메뉴 관리 시스템 완성 (Full CRUD & Interactive UI)**:
    - **메뉴 수정 기능 추가**: 기존 메뉴의 이름, 가격, 설명을 수정할 수 있는 비즈니스 로직 및 UI 구현 완료.
    - **인터랙티브 옵션 매핑**: 메뉴 등록 직후 해당 메뉴에 필요한 옵션 그룹을 즉시 선택하여 연결할 수 있는 '연속 설정 프로세스' 도입.
    - **UI 가시성 대폭 개선**: 수정/삭제/설정 시 현재 등록된 메뉴 목록을 자동으로 미리 출력하여 ID 번호 입력의 번거로움과 실수 방지.
- **메뉴 옵션 시스템 완성 (Final Touch)**:
    - **논커피 옵션 선택 오류 해결**: 개별 메뉴에 옵션이 매핑되지 않은 경우에도 해당 카테고리의 기본 옵션 정보를 상속받는 **Fallback 로직**을 구현하여 모든 상품군에서 옵션 선택이 가능하도록 보강.
    - **객체 그래프 로딩 최적화**: `MenuRepository`에서 옵션 그룹 로딩 시 연관된 세부 옵션(MenuOption)들을 한꺼번에 채워주는 로직을 추가하여 데이터 무결성 확보.
- **진행 현황 업데이트**:
    - 사용자 기능: '상품 선택 및 장바구니 담기' 상태를 완료(Checked)로 변경.

### [2026-03-16] 포인트 적립 비즈니스 로직 완성 및 DB 연결 환경 최적화
- **포인트 적립 자동화 및 내역 연동**:
    - **회원가입 보너스 구현**: 신규 회원가입 성공 시 자동으로 **1,000 포인트**를 적립하고, 해당 내역을 `POINT_HISTORY`에 기록하는 Service 로직 완성.
    - **통합 포인트 업데이트 (`updatePoint`)**: 포인트 잔액 수정과 변동 히스토리 저장을 하나의 서비스 메서드(`MemberService.updatePoint`)로 통합하여 데이터 정합성 확보.
- **데이터베이스 연결 설정 복구 및 안정화**:
    - **`dbInfo.properties` 이슈 해결**: 누락된 DB 설정 파일을 복구하고, `src` 및 `bin` 폴더 내 경로 최적화를 통해 `DBUtil` 로딩 에러 해결.
    - **예외 처리 강화**: `MemberController`의 로그인 로직에서 시스템 오류 발생 시 `System.err` 로그를 남기도록 보완하여 디버깅 편의성 증대.
- **진행 현황 업데이트**:
    - 회원 기능 중 '회원가입 보너스' 및 '포인트 내역 확인' 기능에 대한 완료(Checked) 처리 반영.

### [2026-03-15] 포인트 관리 고도화, 관리자 보안 시스템 구축 및 지능형 등록 개선
- **포인트 투명성 강화 (History System)**:
    - `POINT_HISTORY` 테이블 및 모델을 신설하여 모든 포인트 변동(지급/차감/사용/적립) 내역을 사유와 함께 기록.
    - 회원이 로그인 후 본인의 포인트 변동 사유(예: "회원 생일 이벤트 🎁")를 직접 확인할 수 있는 메뉴 추가.
    - 관리자가 포인트 수정 시 **'수정 사유'를 필수 입력**하도록 UI 및 비즈니스 로직 확장.
- **관리자 계정 보호 및 1인 관리자 정책**:
    - **1인 관리자 제한**: 시스템 내 `ADMIN` 역할을 가진 회원은 단 1명만 존재하도록 제한하여 권한 오남용 방지.
    - **삭제 보호**: 관리자 계정은 직접 삭제가 불가능하며, 반드시 `USER` 등급으로 변경한 후에만 삭제 가능하도록 안전 장치 구현.
    - **수정 보호**: 관리자 등급의 회원은 포인트 수정 대상에서 제외하여 데이터 무결성 확보.
### [2026-03-15] 매출 통계 통합 분석, 데이터 리포팅 및 협업 거버넌스 확립
- **매출 통계 및 분석 시스템 정문화**:
    - **피크타임 통합 분석**: 시간대별 및 요일별 매출 추이를 한 번의 조회로 통합 분석할 수 있는 입체적 리포트 기능 구현.
    - **데이터 내보내기 (Export to CSV)**: 분석된 모든 통계 데이터를 엑셀에서 활용 가능한 CSV 파일로 추출 및 `MS949` 인코딩 적용으로 한글 호환성 확보.
    - **리포트 보관 및 보안**: `resources/cafe_sales_report/` 전용 폴더 생성 및 `.gitignore` 설정을 통한 분석 데이터의 외부 노출 방지.
- **포인트 투명성 및 보안 정책 강화**:
    - **History System**: 모든 포인트 변동(지급/차감/사용/적립) 내역을 사유와 함께 기록하고 회원이 직접 조회할 수 있는 투명성 확보.
    - **1인 관리자 정책 (Singleton Admin)**: 시스템 내 관리자 권한 남용 방지를 위해 단 1명의 관리자만 존재할 수 있도록 제한 로직 구현.
    - **계정 보호 로직**: 관리자 계정의 직접 삭제 및 포인트 수정을 원천 차단하여 시스템 데이터의 무결성 보장.
- **협업 규칙 및 브랜치 전략 공식화 (Section 7 신설)**:
    - **브랜치 운영 원칙**: 기능 단위 구현 완료 시 `develop` 병합 권장 및 **팀원 간 사전 의견 조율 의무화** 규칙 도입.
    - **개발 표준 준수**: 모든 소스 및 DB 환경에 **UTF-8** 인코딩 강제 적용 및 민감 정보(`dbInfo.properties`) 공유 금지 수칙 수립.
- **주문 관리 및 옵션 정책 최적화**:
    - **상세 주문 리포트**: 주문 시간 라벨링 및 상세 메뉴/선택 옵션을 계층형(`└─`)으로 출력하여 관리 가독성 극대화.
    - **지능형 매핑 고도화**: '라떼/프라푸치노' 등록 시 사이즈/휘핑 옵션 동시 자동 설정 로직 보완.
    - **카테고리별 디폴트 옵션**: 커피(5종), 논커피(3종), 디저트(0종)로 카테고리별 특성에 맞는 기본 옵션 셋 재정립.
- **환경 설정 최적화**:
    - 프로젝트 리네이밍(`1_Cafe_kiosk_Projects`) 및 이클립스 `Build Path` 설정 자동화(`.classpath` 보완).

### [2026-03-13] 주문 취소 포인트 복구 시스템 및 관리자 UI/UX 고도화
- **지능형 메뉴 등록 로직 도입**:
    - 메뉴 이름에 특정 키워드(**'라떼', '프라푸치노'**)가 포함된 경우 '휘핑유무' 옵션 그룹을 자동으로 매핑하는 지능형 서비스 로직 구현.
    - 이를 통해 관리자의 반복적인 옵션 매핑 작업을 자동화하고 데이터 등록 생산성 향상.
- **주문 취소 및 포인트 정합성 보장**:
    - `CANCELLED` 처리 시 **트랜잭션(Transaction)**을 적용하여 상태 변경과 포인트 업데이트를 원자적으로 처리.
    - 사용자가 주문 시 사용했던 포인트는 **반환**하고, 주문으로 인해 적립되었던 포인트는 **자동 회수**하는 비즈니스 로직 구현.
- **관리자 가독성 및 조작 편의성 개선**:
    - **주문 관리**: 주문 목록 조회 시 회원 ID 대신 **휴대폰 번호**를 출력하도록 JOIN 쿼리 및 모델 확장.
    - **메뉴 관리**: 메뉴 목록 출력 시 실제 **데이터베이스 ID(`[메뉴 ID: X]`)**를 명시적으로 노출하여 삭제/수정 시 식별성 강화.
    - **안전 장치**: 모든 관리자 입력 단계(삭제, 수정 등)에서 **'취소(0)'** 기능을 도입하여 사용자 실수 방지.
- **시스템 안정화**:
    - 카테고리 상세 조회 시 발생하는 SQL 조인 오류 해결 및 단계별 조회 로직으로 리팩토링.
    - `CATEGORY_OPTION_GROUP` 테이블 부재 이슈 해결 및 DDL 가이드 보완.

### [2026-03-13] 관리자 매출 통계 분석 고도화 (Business Intelligence)
- **통계 데이터 가공 및 분석 로직 확장**:
    - **기간별 상세 조회**: 시작일과 종료일을 입력받아 주문 건수, 총액, **객단가(AVG)**를 산출하는 로직 구현.
    - **시간대별 매출 분석**: `HOUR()` SQL 함수를 사용하여 하루 중 피크타임을 시각화(막대 그래프)하는 기능 추가.
    - **VVIP 회원 분석**: 누적 결제액 기준 상위 5명의 회원 리스트와 기여도를 분석하여 마케팅 지표 제공.
- **Repository 및 Service 계층 강화**:
    - `OrderRepository`에 `getSalesStatsByPeriod`, `getHourlySales`, `getTopSpenders` 메서드 추가 및 구현.
    - `AdminService`를 통해 위 기능들을 관리자 메뉴와 연동.
- **데이터 정합성 관리 도구 강화**:
    - 관리자의 오입력 데이터 수정을 위한 **옵션 그룹 및 세부 옵션 삭제** 기능 UI 연동 완료.

### [2026-03-13] 카테고리별 옵션 체계 고도화 및 팀 프로젝트 통합
- **카테고리 기반 옵션 관리**:
    - `CATEGORY_OPTION_GROUP` 테이블 도입을 통한 카테고리 단위 옵션 일괄 설정 구현.
    - 커피(온도/사이즈/카페인), 논커피(온도/사이즈/휘핑) 등 카테고리별 기본 옵션 제공 규칙 정의.
- **Menu 모델 및 연동 로직 강화**:
    - `Menu` 모델에 `List<OptionGroup>` 필드를 추가하여 메뉴 조회 시 해당 메뉴에 적용 가능한 옵션 정보를 함께 로드하도록 개선.
    - `MenuRepositoryImpl`에서 `fetchOptionGroups` 메서드를 통해 객체 그래프 탐색 구조 구현.
- **Repository & Service 고도화**:
    - `CategoryRepository`에서 `LEFT JOIN`을 사용하여 카테고리와 옵션 그룹 정보를 일괄 로드하도록 개선.
    - `Category` 모델 내부에 옵션 그룹 리스트(`List<OptionGroup>`)를 포함하여 객체 지향적 구조 강화.
- **UI/UX 및 관리자 기능 확장**:
    - `EndView`의 메뉴 목록 출력 시 각 메뉴별 '이용 가능한 옵션' 정보를 시각적으로 표시하여 사용자 편의성 증대.
    - `MenuView`의 카테고리 관리 메뉴 내에 '옵션 그룹 매핑 설정' 기능을 추가하여 실시간 규칙 제어 가능.
- **팀 최신 기능 병합**:
    - 팀 저장소(`develop`)의 `OrderingView` 및 최신 검색/조회 기능들을 성공적으로 병합 및 통합 완료.

### [2026-03-13] 데이터베이스 예약어 충돌 해결 및 관리자 기능 고도화
- **DB 아키텍처 개선**:
    - `OPTION` 테이블명을 `MENU_OPTION`으로 변경하여 예약어 충돌 이슈 원천 차단.
    - `Option.java` → `MenuOption.java` 리팩토링 및 오타 수정.
- **관리자 기능 계층화**:
    - 메뉴 관리를 '메뉴 정보 관리'와 '메뉴 옵션 관리'로 분리.
    - 옵션 그룹 및 세부 옵션에 대한 독자적인 CRUD 체계 구축.

### [2026-03-12] AdminController 리팩토링 및 MVC 패턴 고도화 (상세 메모 복구)
- **Controller 구조 분리**:
    - `AdminController`를 도입하여 View(UI)와 Service(로직) 사이의 흐름 제어를 전담하도록 설계.
    - 컨트롤러 내에서 모든 예외(`Try-Catch`)를 처리하여 사용자에게 일관된 `FailView` 메시지 전달.
- **View 계층 정문화 (Static 메서드화)**:
    - `EndView`: 성공 메시지 및 데이터 목록(카테고리/메뉴/회원)의 시각적 출력 전담.
    - `FailView`: 모든 에러 상황에 대한 안내 문구 출력 전담.
    - `MenuView`: 관리자 전용 무한 루프 및 입력 처리를 담당하여 비즈니스 로직과 UI 분리.
- **Service 인터페이스 및 구현체 강화**:
    - `AdminService` 인터페이스와 `AdminServiceImpl` 구현체를 분리하여 유연한 확장 기반 마련.
    - 주문 취소(`cancelOrder`) 등의 결과에 따라 `boolean` 타입을 반환하여 컨트롤러에서 분기 처리 가능하게 개선.

### [2026-03-12] DB 데이터 무결성 제약 조건 강화 및 UX 개선 (상세 메모 복구)
- **데이터 무결성 확보**:
    - `CATEGORY` 테이블의 `category_name`에 `UNIQUE` 제약 조건 추가로 중복 등록 원천 차단.
    - `MENU` 테이블의 `menu_name`에도 `UNIQUE` 제약 조건을 추가하여 상품명 중복 방지.
- **주문 관리 UI 고도화**:
    - 주문 목록 조회 시 상태별 마킹 도입 (`[V]` 완료, `[X]` 취소).
    - '폰번호' 필드명을 사용자 친화적인 '주문자'로 변경 및 출력 포맷 최적화.
    - 주문 취소 로직: `COMPLETED` 상태인 주문만 취소 가능하도록 상태 기반 검증 추가.
- **메뉴 정보 시각화 개선**:
    - 메뉴 조회 시 카테고리 ID 대신 실제 **카테고리명**이 출력되도록 JOIN 쿼리 및 매핑 로직 반영.
    - 메뉴의 상세 설명(`description`)과 판매 가능 여부를 목록에 함께 노출.
- **등록 로직 안정화**:
    - 카테고리 ID 입력 시 존재하지 않는 ID를 입력할 경우, 즉시 에러 메시지를 띄우고 메뉴판을 다시 보여주는 루프 로직(`While-Try`) 구현.
- **매출 분석 기능 정밀화**:
    - 날짜별 매출 추이: 일별, 주별, 월별, 연도별 분석 기능을 추가하고 `DATE_FORMAT`을 활용한 통계 쿼리 구현.
    - 주차별 데이터 가독성 개선: 'YYYY-MM-W주' 형태로 가공하여 시각적 직관성 확보.

### [2026-03-11] 프로젝트 환경 고도화 및 이클립스 최적화 (상세 메모 복구)
- **데이터베이스 설정 외부화**:
    - `dbinfo.properties` 파일을 도입하여 DB 접속 주소, ID, PW를 소스 코드 외부에서 관리하도록 개선 (보안 및 유지보수성 향상).
- **JDBC 유틸리티 개선**:
    - `DBUtil` 클래스에 Properties 파일을 자동으로 로드하는 정적 초기화 블록 추가.
    - `SQLException` 발생 시 상세한 로그를 출력하여 디버깅 편의성 확보.
- **환경 설정 동기화**:
    - 프로젝트 인코딩을 **UTF-8**로 강제 지정하여 한글 깨짐 이슈 방지.
    - `lib` 폴더를 생성하고 `mysql-connector-j-9.6.0.jar` 라이브러리 추가 및 빌드 경로(Build Path) 설정 완료.
- **초기 매출 통계 시스템 구축**:
    - 누적 매출 합계 및 인기 메뉴 Top 3 추출을 위한 초기 쿼리 및 Repository 구현 완료.
---
## 6. 개발 및 협업 가이드 (실행 전 확인)
1.  **MySQL 설정**: `resources/DDL.sql` -> `resources/DML.sql` -> `resources/admin.sql` 순서로 실행
2.  **DB 접속 정보**: `resources/dbInfo.properties` 파일에서 본인의 MySQL 계정/비번 수정
3.  **라이브러리**: `lib/mysql-connector-j-9.6.0.jar`가 Build Path에 포함되어 있는지 확인
4.  **인코딩**: 이클립스 실행 설정(Run Configurations)에서 **Encoding을 UTF-8**로 지정 필수
---
## 7. 협업을 위한 변경사항 및 개발 규칙
- **최신 DDL 반영 및 테이블 구조 최적화**:
  - `ORDERITEM` → `ORDER_ITEM`, `OPTIONGROUP` → `OPTION_GROUP` 등 테이블명 명명 규칙 통일 (Snake Case)
  - `ORDER_ITEM` 테이블에 `menu_name_snapshot`, `category_name_snapshot` 컬럼 추가하여 데이터 정합성 강화 (메뉴명 변경 시에도 과거 주문 내역 보존)
  - `ORDERS` 테이블의 `ordered_at` → `order_date` 컬럼명 변경
- **데이터베이스 운영 규칙 (중요)**:
  - **DDL/DML 관리**: 신규 테이블(`POINT_HISTORY`, `CATEGORY_OPTION_GROUP` 등)이나 초기 옵션 데이터 추가 시 반드시 `resources/admin.sql`에 기록하여 팀원들과 공유한다.
  - **자동 테이블 생성**: `MemberRepositoryImpl`에 포함된 테이블 자동 생성 로직을 확인하고, 원격 DB 환경 변경 시 이를 먼저 실행하도록 한다.
- **설정 및 보안 규칙**:
  - **민감 정보 보호**: `dbInfo.properties` 파일은 개인별 DB 접속 정보를 담고 있으므로 절대 깃허브에 Push하지 않는다. (이미 `.gitignore`에 등록 완료)
  - **파일명 정규화**: 파일 로딩 시 대소문자를 구분하는 환경(Linux 등)을 고려하여 `dbInfo.properties`와 같이 대소문자를 정확히 일치시킨다.
- **개발 표준**:
  - **인코딩**: 한글 깨짐 방지를 위해 모든 소스 파일과 이클립스 실행 환경, 데이터베이스 연결 문자열에 **UTF-8** 인코딩을 강제 적용한다.
  - **브랜치 전략 및 소통 규칙 (필독)**:
  - **작업 브랜치**: 개인 작업은 각자의 `feature/` 브랜치에서 진행 후 PR(Pull Request)을 통해 병합한다.
  - **Develop 커밋 시점**: `develop` 브랜치에는 개별 기능 단위로 구현 및 테스트가 완벽히 완료되었을 때만 작업 내역을 커밋/푸시하는 것을 권장한다.
  - **의견 조율 필수**: `develop` 브랜치에 직접 커밋하거나 푸시해야 할 상황이 발생할 경우, **반드시** 사전에 팀원들과 충분한 의견 조율을 거친 후 실행한다.
